const myLibrary = [];

function Book(title, author, pages) {
    this.title = title;
    this.author = author;
    this.pages = pages;
    this.read = false;
}

function addBookToLibrary() {
    // get user input to create a new Book object
    const book = undefined;
    myLibrary.push(book);
}